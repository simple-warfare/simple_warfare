use bevy::prelude::*;
use parry2d::shape::SharedShape;
use vleue_navigator::prelude::ObstacleSource;
/*


impl ObstacleSource for SharedShape {
    fn get_polygons(
        &self,
        obstacle_transform: &GlobalTransform,
        navmesh_transform: &Transform,
        up: (Dir3, f32),
    ) -> Vec<Vec<Vec2>> {
        todo!()
    }
}

*/
