
use bevy::prelude::*;

pub struct TrickFilmPlayerPlugin;

impl Plugin for TrickFilmPlayerPlugin {
    fn build(&self, app: &mut App) {}
}

fn handle_trick_film_player_event(asset_server: Res<AssetServer>) -> Result {
    Ok(())
}
